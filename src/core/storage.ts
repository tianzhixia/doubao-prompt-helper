import type { PromptTemplate } from "../types";

export async function loadTemplates(): Promise<PromptTemplate[]> {
  return [];
}

export async function saveTemplates(_templates: PromptTemplate[]): Promise<void> {
  return;
}
